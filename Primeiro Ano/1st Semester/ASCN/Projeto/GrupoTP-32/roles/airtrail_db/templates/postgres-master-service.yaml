apiVersion: v1
kind: Service
metadata:
  name: postgres-master
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: postgres
    role: master
  ports:
    - port: {{ postgres_port | int }}
      targetPort: {{ postgres_port | int }}
